<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Promemoria diario (RF22): allarmi esatti via AlarmManager. Su Android 13+ (API 33+)
         basta USE_EXACT_ALARM, concesso all'installazione senza prompt; su Android 12 (API
         31-32) serve SCHEDULE_EXACT_ALARM, auto-concesso ma limitato a quelle versioni. -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:name=".NutreaseApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nutrease">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Promemoria diario via allarmi esatti (AlarmManager): allo scatto dell'allarme
             questo receiver mostra la notifica e ri-arma per la settimana successiva. Non esportato. -->
        <receiver
            android:name=".data.receiver.ReminderAlarmReceiver"
            android:exported="false" />
    </application>

</manifest>