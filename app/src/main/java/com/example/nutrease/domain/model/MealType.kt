package com.example.nutrease.domain.model

/** Categoria del pasto (RF9); l'etichetta italiana per la UI vive in `ui/screens/diary`. */
enum class MealType {
    BREAKFAST,
    LUNCH,
    SNACK,
    DINNER
}